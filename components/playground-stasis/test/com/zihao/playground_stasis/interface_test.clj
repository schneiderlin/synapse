(ns com.zihao.playground-stasis.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.zihao.playground-stasis.interface :as playground-stasis]))

(deftest dummy-test
  (is (= 1 1)))
